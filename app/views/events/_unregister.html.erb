<%= profile_for(user) %>
<%= form_for(@event.participations.find_by(user_id: user.id),
             html: { method: :delete }) do |f| %>
 <div><%= hidden_field_tag :user_id, user.id %></div>
 <div><%= hidden_field_tag :event_id, @event.id %></div>
  <%= f.submit "Unregister", class: "btn" %>
<% end %>