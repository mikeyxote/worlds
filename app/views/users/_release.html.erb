<%= form_for(current_user.active_managements.find_by(managed_id: user.id),
             html: { method: :delete }) do |f| %>
  <div><%= hidden_field_tag :managed_id, user.id %></div>
  <%= f.submit "Release", class: "btn" %>
<% end %>